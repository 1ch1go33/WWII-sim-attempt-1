import pygame
class Button:
    global X
    global Y
    def DetectMouse(self):
        if (X == self.X) and (Y == self.Y):   #this isn't done yet
            print('mouse is yes')

LoadGame = Button()
